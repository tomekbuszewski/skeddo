{
  "name": "skeddo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "oxlint --fix",
    "format": "oxfmt",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "turbo": "^2.9.6",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24"
  }
}
